import 'package:flutter/material.dart';

class TrackOneScreen extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return new Center(
      child: new Text(
        "Track1",
        style: new TextStyle(fontSize: 20.0),
      ),
    );
  }
}