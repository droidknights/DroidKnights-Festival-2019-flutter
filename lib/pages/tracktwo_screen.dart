import 'package:flutter/material.dart';

class TrackTwoScreen extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return new Center(
      child: new Text(
        "Track2",
        style: new TextStyle(fontSize: 20.0),
      ),
    );
  }
}